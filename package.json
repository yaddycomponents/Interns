{
  "name": "interns-showcase",
  "version": "1.0.0",
  "description": "Intern projects showcase",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx serve public",
    "dev": "npm run build && npm run serve"
  }
}
